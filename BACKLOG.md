# Backlog — DCS Video Manager

> Last updated: May 2026

---

## Bugs

| # | Title | Priority | Description |
|---|-------|----------|-------------|
| 1 | Tags fail in OAuth Testing mode | 🔴 High | Investigate whether this is a permanent Google limit for unverified apps or if there is a workaround |
| 3 | No real-time upload progress | 🔴 High | The progress bar does not advance during upload — it stays fixed until the upload finishes |

---

## Features

| # | Title | Priority | Description |
|---|-------|----------|-------------|
| 8  | Descriptions adapted to video length | 🔴 High | Short videos (<5 min) should have a different description than long ones — Gemini must know the duration before generating |
| 30 | TacView ACMI event extraction | 🔴 High | Parse the `.acmi` file generated by TacView (text format, stable across versions) and extract key events: SAM launches/evasions, kills, near-misses, BVR engagements with exact timestamps. Inject extracted events into the Gemini prompt as tactical context (`"1 kill at 02:15, 2 SAM evasions, BVR engagement 04:47–07:17"`) for more accurate descriptions and chapters. Also feeds #13 (Shorts) and #12 (Debrief). Note: requires TacView with ACMI auto-record enabled. Parser: `acmi-parser` (PyPI). |
| 9  | Series/campaign detection and episode numbering | 🟡 Medium | Detect in `history.json` whether the video belongs to an ongoing campaign and suggest the correct episode number. Also auto-link to previous videos in the same campaign by querying the channel's uploaded videos via the YouTube Data API (`search.list` with channel filter). |
| 10 | Automatic chapters via audio analysis | 🟡 Medium | ffmpeg detects silences and phase changes (briefing→takeoff→combat→landing) to generate more accurate chapters |
| 12 | Debrief report for E111 | 🟡 Medium | Generate a mission summary in military report format to share on the squadron Discord/forum. Output example: aircraft, map, mission result (RTB/crash/complete), duration, kills, SAM evasions, max Mach, max altitude, fuel remaining, plus a narrative paragraph generated by Gemini. Accuracy improves significantly when TacView data (#30) is available, but Gemini can produce a baseline report from frames alone. |
| 13 | Automatic YouTube Shorts generation | 🟡 Medium | Detect action moments via audio peaks (ffmpeg) and, when TacView data (#30) is available, use its event timestamps (kills, SAM evasions, BVR engagements) as clip markers. Extract 3–5 clips of 30–60 s each, crop to 9:16, generate Short metadata with #Shorts. Present a grid in the UI so the user can select which clips to publish before uploading. |
| 16 | Schedule publish date and time | 🟡 Medium | Add a date/time picker to schedule publication instead of doing it manually from YouTube Studio |
| 18 | Channel stats dashboard | 🟢 Low | Visualise most-recorded modules, ongoing campaigns, and video upload history from `history.json` |
| 19 | Duplicate detection | 🟢 Low | Compare the current video against the history and warn if a recording of the same mission/campaign was already uploaded |
| 21 | Customisable description templates | 🟢 Low | Edit description templates (English/Spanish) from the UI without touching the code |
| 31 | OBS scene metadata context | 🟡 Medium | Read the OBS scene name and scene change timestamps from MKV file metadata and pass them to Gemini as context (e.g. `"Briefing scene: 0:00–1:30, Combat scene: 1:30–18:45"`). Requires a one-time OBS Lua/Python script setup that writes chapter markers or a `DESCRIPTION` tag to the recording at scene-change events. Implementation in the app is low-cost (ffprobe already used). |

---

## UX

| # | Title | Priority | Description |
|---|-------|----------|-------------|
| 24 | History with metadata preview | 🟢 Low | Clicking a video in the history tab shows its full saved metadata from the JSON file |
| 25 | Dark/light mode toggle | 🟢 Low | The UI is always in dark mode. Add a toggle to switch to a light theme |

---

## Infra

| # | Title | Priority | Description |
|---|-------|----------|-------------|
| 17 | Batch mode with folder watcher | 🟡 Medium | Monitor the DCS Saved Games recordings folder with `watchdog` (Python). When new `.mkv` files appear, queue them automatically for analysis using the existing async job infrastructure. Display the queue in the web UI. The user reviews and approves each result before upload — no automatic publishing. Consolidates previous items #17 (Batch mode) and #20 (Folder watcher). |
| 26 | Support additional modules in prompt | 🟡 Medium | Add F-14, UH-1H, and A-10C with their specific characteristics to the Gemini prompt context |
| 27 | Editable config from UI | 🟡 Medium | Edit `config.json` (links, frames, model) from the Setup tab without touching files manually |
| 28 | Export metadata as CSV | 🟢 Low | Export the full history as CSV for analysis or external backup |
